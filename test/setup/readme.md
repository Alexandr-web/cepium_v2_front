Папка `test/setup/` предназначена для хранения разных моковых данных, которые могут использоваться в разных тестах.

При создании файла в этой директории подключите его через свойство `setupFiles` в конфигурации `vitest.config.ts`.